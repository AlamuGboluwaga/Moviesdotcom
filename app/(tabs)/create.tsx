import { View, Text } from "react-native";
import React from "react";
import Layout from "../(auth)/_layout";

const Create = () => {
  return (
    <Layout>
      <View><Text className='text-white'>Create</Text></View>
    </Layout>
  );
};

export default Create;
