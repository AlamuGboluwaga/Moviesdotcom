import { View, Text } from 'react-native'
import React from 'react'
import Layout from '../(auth)/_layout'

const Saved = () => {
  return (
       <Layout>
      <View ><Text className='text-white'>Saved</Text></View>
    </Layout>
  )
}

export default Saved